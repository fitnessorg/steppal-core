// TODO(contributor): Flesh this out once the Phase 2 schema (users, pots,
// pot_members, step_days) lands. It should insert three users, one active
// pot with those users as members, and a week of plausible step_days rows so
// a fresh clone has something to look at immediately. Done = `pnpm db:seed`
// on an empty database leaves a pot visible via GET /v1/pots for a seeded user.
// difficulty: easy
console.log("Nothing to seed yet — schema lands in Phase 2.");
